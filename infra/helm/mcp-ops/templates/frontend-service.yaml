apiVersion: v1
kind: Service
metadata:
  name: mcp-ops-frontend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: mcp-ops-frontend
  ports:
    - port: {{ .Values.frontend.servicePort }}
      targetPort: {{ .Values.frontend.servicePort }}
